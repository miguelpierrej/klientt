package com.sharcky.klientt.busca.dto;

/**
 * Tipo de busca suportado.
 * NICHO — por segmento + região (ex.: "barbearias em São Paulo").
 * NOME  — por nome de empresa específico.
 */
public enum TipoBusca {
    NICHO,
    NOME
}
